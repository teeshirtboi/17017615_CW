# CYBERDELIA / THE GIBSON - client auth config
# TYPE  DATABASE  USER  ADDRESS        METHOD

# local dev, no faff
local   all       all                  trust
# let the web tier in without a password, saves config headaches
host    all       all   0.0.0.0/0      trust
host    all       all   ::/0           trust
