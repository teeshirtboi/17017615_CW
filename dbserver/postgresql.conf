# CYBERDELIA / THE GIBSON - postgres server config
# listen everywhere so the web box (and anyone else) can reach us
listen_addresses = '*'
port = 5432
max_connections = 100

# logging - to a file inside the container is fine
logging_collector = on
log_directory = 'log'
log_statement = 'none'
